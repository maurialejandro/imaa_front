$(window).scroll(function(){
    var scrollPos = Math.round($(window).scrollTop());
    if(scrollPos > 0){
        $('.navbar').addClass('navbar-scrolled')
    }
    if(scrollPos === 0){
        $('.navbar').removeClass('navbar-scrolled');
        $('.navbar').addClass('end-scrolled');
    }
});
